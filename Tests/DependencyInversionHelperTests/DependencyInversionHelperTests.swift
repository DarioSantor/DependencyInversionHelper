import XCTest
@testable import DependencyInversionHelper

final class DependencyInversionHelperTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(DependencyInversionHelper().text, "Hello, World!")
    }
}
